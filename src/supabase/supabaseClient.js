// This will be used when we connect to Supabase
// import { createClient } from '@supabase/supabase-js'
// import { SUPABASE_URL, SUPABASE_ANON_KEY } from '../configs/config'

// export const supabase = createClient(SUPABASE_URL, SUPABASE_ANON_KEY)

export const supabase = null; // Placeholder for now
